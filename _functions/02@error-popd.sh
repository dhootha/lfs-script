#!/bin/bash
################################################################################
# Функция "error-popd"
# Version: 0.1

error-popd ()
{
ERR_FLAG="${?}"
if [ "${?}" -ne 0 ]; then
#	ERR_FLAG="${?}"
	popd
	return "${ERR_FLAG}"
fi
}

################################################################################
